{
  "name": "lab-5",
  "version": "1.0.0",
  "description": "A starter project for hosting VuePress sites on GitLab pages.",
  "main": "index.js",
  "author": "samdbeckham",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "vuepress dev .",
    "build": "vuepress build .",
    "deploy": "  export NODE_OPTIONS=--openssl-legacy-provider && vuepress build && git add -A && git commit -m 'bot commit' && git push origin master --force"
  },
  "dependencies": {
    "vuepress": "^1.5.3"
  }
}